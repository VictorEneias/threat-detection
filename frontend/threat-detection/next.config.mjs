/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // opcional, mas recomendado
  // output: 'export',   // ❌ COMENTADO ou removido!
};

export default nextConfig;